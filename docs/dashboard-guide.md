# Dashboard Guide

The CloudWatch dashboard provides a visual overview of the system.

Metrics displayed include:

- CPU utilization
- request latency
- request counts
- error occurrences

The dashboard helps identify abnormal patterns quickly.

If a metric shows unexpected behavior, logs can be inspected to determine the cause.